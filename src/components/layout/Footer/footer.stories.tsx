import type { Meta, StoryObj } from '@storybook/react-vite'
import Footer from '.'

export default {
  title: 'layout/Footer',
  component: Footer
} as Meta

export const Default: StoryObj<typeof Footer> = {}
